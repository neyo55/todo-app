# Enter the Python Shell inside the container:
sudo docker compose exec -it web python

# Type these lines one by one (press Enter after each):
from app import create_app, db
app = create_app()
ctx = app.app_context()
ctx.push()
db.create_all()
exit()
